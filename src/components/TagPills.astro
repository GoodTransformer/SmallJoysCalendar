---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<ul class="tag-pills" aria-label="Activity tags">
  {tags.map((tag) => (
    <li class="tag-pills__item">{tag}</li>
  ))}
</ul>
